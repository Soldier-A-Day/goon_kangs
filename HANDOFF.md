# 이어하기

이 세션에서 무엇을 만들었고, **무엇이 아직 눈으로 확인되지 않았는지**.
새 세션은 이 문서 → `docs/WORKORDER.md`(발주서 원본) 순으로 읽으면 바로 붙는다.

마지막 배포 커밋 기준 · 2026-08-02


## 지금 돌아가는 것

| | 주소 |
|---|---|
| 웹 | https://solider-a-day.vercel.app — Vercel |
| 게임 서버 | https://sad-gameserver.onrender.com — Render 무료 (15분 슬립 → 로비 진입 시 /health 핑으로 깨움, 이번 세션에 넣음) |
| 저장소 | Upstash Redis + Supabase |

배포 검증: 로컬·원격 `web.loader.js` sha256 일치 확인함. 이 저장소는 "코드는 고쳤는데
화면은 옛것" 사고가 반복됐으므로 **배포마다 해시 대조가 관례다.**


## 이 세션에서 한 일 (요약 — 상세는 git log와 WORKORDER)

**문서 둘이 이 세션의 뼈대다:**
- `docs/BENCHMARK.md` — 리서치 16건 종합(포켓몬·동숲·좀비고·아이작·시장·아트 4종·소리·내러티브·온보딩·기술·침묵감사·현지화·확산). 결론: "없는 것은 아이디어가 아니라 마감"
- `docs/WORKORDER.md` — A~H 발주서. 모든 발주에 목표·완료판정·검증·의존·중단기준. **다음 세션은 여기서 집는다**

**배포된 것 (시간순):**
1. HUD 수리 묶음 — 복도 미니맵 프레이밍·지도 범례·배치도 방 이름·미니맵+수첩 붙이기
2. 하루 마감 화면 큐(점호 판정이 안 보이던 버그)·하달 창 가드·"이 방에서 다시" 수리·재접속 시 스냅샷 유실 수리·보일러실↔세면장 문
3. 구제권 표시(판정 화면) — 그리고 설계 어긋남 발견: 기획서는 발동형, 코드는 자동 카운터 (WORKORDER B-4)
4. A단계 배치: **F9 미니게임 실험대**(+`?lab=1` URL 진입, 첫 입력 전 시간 정지) · 소리 4종+플래시 베이스 승격 · 봇 수리(balance 6/6 사상 첫 그린 — sim은 무죄, 봇 회복 타이밍이 범인) · **재접속 백오프**(끊기면 4회 재시도+배너) · 해금 실시간 게이트(15/30/40분, 스킵 투표 면역)
5. 하달 창 조기 종료(전원 확정 시) + "하달 대기 — 시간 정지 (N초)" 표시 + 서버 틱 방탄
6. 실험대 피드백으로 잡은 버그: SORT 정답-이름 무관 버그·죽은 "4번" 통·civilaid 변형 누락 / HOLD 열자마자 이탈 누적 / 판 열려 있을 때 Space가 일과표 토글하던 것
7. 마지막 배치(방금 배포): **① 게임이 말한다**(침묵 토스트 12종·하달 말풍선·거부 사유 9종 부활·구제권 상시 표시·퇴소 화면 소대장 대사·UI배율 실동작·색각 마커) · **② 아트**(발밑 그림자·색상 이동 램프 12계열·소품 광원 통일·바닥 반복 깨기) · **③ 캐릭터**(위상 어긋내기·idle 호흡·눈 깜빡임·완료 팝·상태 틴트·걸음 스쿼시) · **④ 초대 7→3단계**(`/join/코드`·링크 복사·보직 자동 배정)


## 8/3 새벽 자동 파이프라인 — WORKORDER 실행 가능 항목 전부 소진

배치 5개(미니게임 2차 → B협동 → C·E → D·F·A-5 2차 → H·T)를 순차 배포했고 전부 해시 대조
완료. **남은 것은 전부 사람 몫이다**: PLAYTEST A/B(F9 전수·4인 협동)·번역 발주(H-3 본문)·
Sentry DSN 발급(env만 넣으면 켜짐)·데이터가 쌓여야 하는 밸런스 판단.
버림 확정: C-2·G단계·H-2(재론 안 함).

**마지막 배치(H·T) 검증 체크리스트:**
- [ ] 미니게임 전 원형이 키보드만으로 깨지는가(화살표 가상 커서/포커스 + Space) — SCRUB·AUDIT·TRACE·SEARCH·TRACK·SEQ 글자 변형
- [ ] 게임 화면 우상단 카메라 버튼 → PNG 다운로드(검은 화면이면 preserveDrawingBuffer 회귀 — 보고)
- [ ] F10 ScreenLab — 점호 7종·마감 큐·하달·종료 4종·토스트 17종·배너·게이지를 서버 없이 표시
- [ ] F11 WorldLab — 구역 텔레포트·날씨/주야 표시 오버라이드(배지)·VFX·캐릭터 뷰어·SFX 시청
- [ ] F9 확장 — [I]인터럽트·[P]연쇄판·[J]합동(가짜 인원)·[G]RaceBoard
- [ ] HUD 시간대 스킵 투표 버튼이 실제로 서버 voteSkip을 보내는가

## 다음 할 일 (순서대로)

0. ~~A-5 1차~~ **완료·배포됨** (이 세션). Sonnet 5개 병렬 워크트리로 구현 — 타이머 결과 매핑
   분리(SCRUB·SEARCH·자유 PLACE는 Fill≥50%면 등급 낮춰 통과, B는 Fill≥85%, A 불가 /
   AUDIT·SORT 하드 유지) · SCRUB 2~3층 벗기기+덩어리 완료 신호+거울 김 서림(0.5초 예고) ·
   AUDIT 12% "이상 없음이 정답"+상시 신고 버튼+총수 누설 봉인 · SEARCH 실패 리플레이+발견 카드 ·
   TRACE 0.1초 회전 트윈+회전음(Sfx에 pitch 파라미터) · 연속 A 시 제한시간 −5%/스택(최대 3,
   배지로 명시, GradeStreak.cs, F9 랩은 무영향) · 같은 원형 2연속 배치 회피(sim quests.ts
   `avoidAdjacentMinigame`, rng 소비 순서 불변 — 저장 호환 유지).
1. **F9 전수 플레이 계속** — 14종 중 사람이 본 것은 일부다. 지금까지 3판에서 버그 3개 나옴.
   이번 개편 4종(SCRUB·AUDIT·SEARCH·TRACE)이 최우선.
3. **E-2** — 서버가 명시적으로 버리는 `conditionCritical` 살리기 + 간부 신뢰도 3트랙 프로토콜 노출(스키마에 아예 없음, `bestOfficer` 죽은 코드).
4. **B단계 협동** — 정보 비대칭·위기와 구조·quick-phrase·구제권 발동형(B-4). PLAYTEST A 통과가 게이트.
5. **A-5 2차**(PLACE 전면 개편·청각화·신규 원형) — D-4 사운드 확장과 묶어서.
6. **T단계 테스트 랩**(ScreenLab F10·WorldLab F11·F9 확장) — **전부 구현 뒤 마지막에** (사용자 결정).

에이전트 운용 관례(사용자 지시): 오케스트레이터는 지휘만, 구현·검증은 Sonnet 병렬 워크트리,
파일 소유권을 발주에 명시해 충돌 0으로 병합, 빌드는 마지막에 한 번.


## 사람이 눈으로 확인할 것 (미검증 — 이 세션 마지막 배포분)

강력 새로고침(⌘⇧R) 후:

**D·F·A-5 2차 배치 (8/3 새벽 배포)**
- [ ] 소리 — 걸을 때 발소리 / 판 열림·닫힘음 / 위기 알람 / TIMING 접근음(가까울수록 높고 촘촘) / SEQ 단계별 계이름 / HOLD 통과 콤보 피치 상승
- [ ] 소품 — 같은 그림이던 소품들(서류함·약제함·잔반통 등 35종)이 코너 표식으로 구분되는가
- [ ] 신규 원형 — F9에서 STILL(부동자세: 참다가 "해산"에만 반응)·DODGE(3레인 회피)·RaceBoard([G]키, 고스트 진행 바) 동작하는가 / 실전에서 불시 점호=STILL·정신교육=STILL·보급 트럭=DODGE로 나오는가
- [ ] 온보딩 — 첫 1~2일 필수 일과가 기본 원형(문지르기·배치·대조 등)만 나오는가 / 이등병은 하달을 못 받고 사유가 뜨는가 / 취침 정산에 "내일 예고" 한 줄 / 청결 링에 판정 문턱선 / 1~2인 방은 기록에 안 남는가
- ⚠ 저장 v3 — 옛 저장 이어하기 불가(의도)

**C·E 배치 (8/3 새벽 배포)**
- [ ] 퇴소 화면 상단에 런 헤드라인("한파 속 물자 부족 D-5 완주" 형식)이 뜨는가 / 실패 시 결정타 수치("위생 18/20 · N일차부터")가 붙는가
- [ ] 점호 실패 시 조건별 소대장 대사가 뜨는가 / 후송 시 "남은 셋이 그 몫까지" 문장 / 돌발 시작에 상황 도입 문장 / 무전 끊김 문구가 통신병과 나머지에게 다른가
- [ ] 취침 정산의 군기 줄 아래 "합동무결 +12 · 선택미완 -8" 식 분해가 뜨는가
- [ ] 수첩(TAB)에 "오늘의 기록" 탭 — 그날 판정·후송·구제·보급이 시간순으로 쌓이는가
- [ ] (스키마만) 주간 변조 4종(한파·보급·검열·훈련)이 런마다 하나 배정 — 화면 노출은 아직 없음(후속)

**B단계 협동 (8/3 새벽 배포 — 4인/2인 실플레이 필요)**
- [ ] 합동판(SEQ·TRACE 계열)에서 한 명은 정답만·한 명은 조작만 보이는가 — 말 없이는 못 깨는가 (혼자면 기존 판 그대로여야 정상)
- [ ] 동료 스탯 붕괴 시 즉시 후송이 아니라 "쓰러졌다 — N초" 위기 토스트가 뜨고, 곁에서 E 홀드로 구조되는가 / 의무병이 더 빠른가 / 방치하면 기존 후송 그대로인가
- [ ] C키로 정형 문구 8종 휠이 열리고 숫자/클릭 발화 → 머리 위 말풍선이 뜨는가 / 연타 도배가 안 되는가(2초 쿨다운)
- [ ] 분대장 수첩에서 필수 퀘스트 옆 "구제 발동" 버튼 → 필수→선택 강등되는가 / 자격 없는 사람은 사유 토스트가 뜨는가
- [ ] 판정 화면 구제 문구가 "발동" 어조로 바뀌었는가 (자동 상쇄 문구가 아니라)
- ⚠ 저장 포맷 v3 — 진행 중이던 저장 런 이어하기 불가(의도된 무효화)

**미니게임 전면 개편 2차 (8/3 새벽 배포 — F9에서 판별 가능)**
- [ ] PLACE 정돈·배치 — 타일이 좌우로 미끄러지다 클릭/Space에 떨어져 쌓이는가 / 어긋난 만큼 잘려 다음 타일이 좁아지는가 / 배터리 극성(무전기)은 완전 빗나감에 즉시 실패인가
- [ ] HOLD 계기 유지 — 눌러서 붕 뜨고 위/아래 장애물을 피하는가 / 충돌 시 0.8초 무적 깜빡임 후 재개인가
- [ ] SORT 분류 — 테트리스인가(←→ 이동, 클릭/Space 회전, 줄 지우기 목표) / 천장 닿으면 실수 1 + 판 비움인가
- [ ] RHYTHM 박자 — 망치 시연을 보고 같은 리듬으로 따라 박는가 / 엇박이면 못이 삐뚤게 박히는가
- [ ] TRACK — 풍선이 한 번에 하나씩 랜덤 위치에 뜨고 클릭으로 터트리는가 / 수명 끝 0.4초 깜빡임·수축
- [ ] REACT 즉시 반응 — 쉘이 초록("대기")→빨강("지금!")으로 바뀔 때 누르는 방식인가
- [ ] 실험대에서 판 시작 클릭이 판의 첫 입력으로 새지 않는가(SEARCH 헛짚기·AUDIT 오답으로 안 잡히는가)
- [ ] 미니게임 목록의 SEARCH 이름이 "수색"으로 보이는가

**A-5 1차 미니게임 (앞 배포 — F9 실험대에서 판별 가능)**
- [ ] SCRUB — 얼룩이 문지를수록 옅어지다 벗겨지는가(한 번 스치면 끝이 아니라) / 덩어리 하나를 다 닦으면 테두리 번쩍+탭 소리 / 남은 5%에서 은은한 펄스 / 세면장(mirror)에서 80% 넘게 닦으면 파란 예고 깜빡임 0.5초 후 한 덩어리에 김이 다시 서리는가
- [ ] SCRUB 순수 청소(바닥·거울 등)에서 깨끗한 곳을 헛문질러도 실수가 안 쌓이는가 (모래·방역은 약제 낭비 시 기존대로 실수)
- [ ] SCRUB·SEARCH 시간초과 시 "시간 종료 — 한 만큼 인정"+등급(B/C)으로 통과하는가 (절반 미만이면 "절반은 채워야 인정된다" 재시도)
- [ ] AUDIT — 하단 "이상 없음 보고" 버튼이 모든 판에 있는가 / 여러 판 돌리면 가끔 어긋남이 진짜 없는 판이 나오는가 / "적발 n/m"의 총수(m)가 화면에서 사라졌는가
- [ ] SEARCH — 못 찾고 끝나면 1.4초간 놓친 자리가 빨갛게 깜빡이는가 / 찾는 순간 "관물대 밑 편지" 같은 카드가 뜨는가
- [ ] TRACE — 관이 딸깍 소리와 함께 0.1초 돌아가는가(즉시 스냅이 아니라)
- [ ] 연속 A를 2번 이상 받으면 판 머리에 "연속 A ×n — 시간 −n%" 배지가 뜨고 실제로 빡빡해지는가 / A 아닌 등급이 나오면 풀리는가
- [ ] 하루 일과표에서 같은 원형(문지르기 등)이 연달아 붙어 나오는 일이 줄었는가

**아트·캐릭터 (이번 배포의 큰 변화)**
- [ ] 화면 첫인상 — 캐릭터·소품 발밑에 그림자가 생겨 바닥에 붙어 보이는가
- [ ] 복도·연병장 바닥 — 균열·얼룩으로 반복이 깨졌는가 / 색이 이상하게 튀는 곳은 없는가(램프 교체 회귀)
- [ ] 분대원 4인의 idle 호흡이 서로 다른 박자인가 (같으면 실패)
- [ ] 눈 깜빡임(정면 2~5초 간격) / 일과 완료 순간 몸이 1.15배 튀는가 / 동상=푸른 틴트·탈진=탁한 틴트
- [ ] 걸을 때 발 디딤에 맞춰 몸이 눌리고 먼지가 퍽 튀는가 / 픽셀 지글거림이 거슬리면 보고(스케일→오프셋 대안 준비됨)

**게임이 말한다**
- [ ] 하달하면 받는 사람 머리 위 말풍선("또 나야?" 등) / 하달 거부 시 사유가 뜨는가
- [ ] 구제권 1장 이하일 때 일과표·수첩에 경고가 상시 떠 있는가
- [ ] 퇴소 화면에 실패 조건 수치 + 소대장 대사가 뜨는가
- [ ] 설정에서 UI 배율 125/150%가 실제로 게임 화면에 먹는가 / 150%에서 겹치는 화면 있으면 보고
- [ ] 미니맵 분대원 마커가 보직별로 모양이 다른가 (원·사각·십자·삼각)

**초대**
- [ ] 대기실 "초대 링크 복사" → 새 브라우저에서 링크 열면 이름만 넣고 입장되는가 (클립보드는 HTTPS 필요 — 실도메인에서)
- [ ] 재방문 시 이름이 자동으로 채워지는가

**앞서 배포됐지만 아직 미확인**
- [ ] 하달 창에서 전원 확정하면 즉시 닫히고 시간이 재개되는가 / 정지 중 "하달 대기 (N초)" 표시
- [ ] 시계가 이유 없이 멈추면 → 배너("재접속 중 n/4차")가 뜨는지. **배너 없이 멈추면 새 버그, 보고**
- [ ] F9 실험대 — 판 고르면 "클릭하면 시작" 대기 → 첫 입력부터 시간. 분류 "4번" 통 사라짐, 계기 유지 첫 클릭부터
- [ ] 점호 판정 6.4초 → 승급 → 취침 순서로 흐르는가 / 구제권 사용 시 주황 문구

**아는 한계 (버그 아님)**
- 그림자는 `_box()` 계열 소품에만 — 나무·깃대·모래주머니 등 14종 커스텀 소품은 아직 없음
- 텔레포트 없이 훈련장까지 걸어가야 함 (WorldLab은 T단계)
- csharpgen `reliefsUsed` 골든 테스트 1건, sim `quests.ts` CareTemplate 타입 1건 — 세션 전부터 빨간 기존 이슈


## 알려진 문제 (이월)

- 소품 낱장 .meta 105장의 WebGL 오버라이드가 Compressed로 박혀 있음 — Sprite2DImport가 Default만 강제. 아틀라스는 비압축 명시라 무관하나 낱장 잔존분 확인 필요 (H-1 발견)
- ~~InterruptBoard TimedOut 관통~~ **해소됨**(A-5 2차)
- **InterruptBoard가 TimedOut/GradesOnTimeout을 관통 안 함** (구 기록 — 위 참조) — 래퍼가 내부 판의 통과·실패를
  자기 Clear/Fail로 다시 선언하는 구조. 지금은 REACT 인터럽트가 BALANCE·TRACE(둘 다 하드
  판정)에만 걸려 증상 없음. A-5 2차에서 과정형에 인터럽트를 얹기 전에 래퍼에 위임을 넣을 것
- **저장 포맷 v2** — 이번 배포로 진행 중이던 저장 런은 이어하기 불가(의도된 무효화)
- 공통 일과가 오전 칸에만 뜨는 것(`pickPhase` 인덱스 0 고정) — 의도인지 실수인지 미확인
- `optionalMissed` 페널티 건수 비례 아님 — WORKORDER 침묵감사 #12
- CORS `*` — 도메인 확정 후 좁히기
- 워크트리 잔재: `.claude/worktrees/`에 세션 한도로 죽은 랩 에이전트 3개의 미완 워크트리가 있을 수 있음 — T단계 시작 때 참고하거나 지울 것


## 명령 (변한 것 없음 — 요지만)

```bash
npm run -w services/gameserver dev     # :8080
npm run dev -w apps/web                # :3000
python3 tools/sprites/generate.py      # 아트 재생성 (2회 실행 비트 동일해야 정상)
# 씬: BaseScene.CreateScene / 빌드: BuildPlayer.Web (성공 신호는 "Exiting batchmode successfully")
node tools/webgl-sync.mjs              # 빌드 후 필수 — 빼먹으면 화면이 옛것
npx tsx tools/csharpgen/src/index.ts   # 프로토콜 고쳤으면 필수
npx vercel --prod --yes                # 웹 배포 — push가 Render 서버 배포 트리거
# 배포 후: 로컬/원격 web.loader.js sha256 대조 (관례)
```

## 참고 문서

| | |
|---|---|
| `docs/WORKORDER.md` | **발주서 원본** — 다음 할 일의 상세는 전부 여기 |
| `docs/BENCHMARK.md` | 리서치 16건 종합 — 왜 이 순서인지의 근거 |
| `docs/JOINT.md` · `docs/game_spec.md` · `docs/QUESTS.md` | 합동 설계 · 미니게임 원형 · 일과(생성물) |
| `SAD-GDD-002_2D기획서.html` | 기획서 — 구제권·점호 6.4초·사운드 §16 등 코드와 대조할 원본 |
