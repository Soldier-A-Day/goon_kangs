# 이어하기

이 세션에서 무엇을 만들었고, **무엇이 아직 눈으로 확인되지 않았는지**.
새 세션은 이 문서 → `docs/WORKORDER.md`(발주서 원본) 순으로 읽으면 바로 붙는다.

마지막 배포 커밋 기준 · 2026-08-03 오전 (8/2~8/3 밤샘 파이프라인 + 오전 재설계까지 전부 배포됨)

**사람이 확인할 때 최신 배포 기준**: 검증 체크리스트는 아래 날짜별 섹션에 쌓여 있다 —
"8/3 오전" 섹션이 최신이고, 그 아래 섹션들(8/3 새벽 배치들)도 대부분 아직 사람 눈 검증 전이다.


## 입체감 D안 (8/5 진행 중 — 이 세션의 현재 작업)

**결정**: HD-2D 도입 안 함. 대신 탑다운 2D에 빠져 있던 깊이 단서를 채운다.
사용자가 목업 4안(A 현재 / B 셰이딩 / C +벽높이 / D +실시간조명)을 보고 **D 채택**.
목업 산출물: 스크래치패드 `room_compare.png`, `room_0~3.png` (세션 종료 시 사라짐 — 재생성은 `room_mock.py`)

**진단 근거 (코드 확인)**
- 월드 스프라이트가 전부 `Sprites/Default` = unlit (`BaseScene.cs:827`) → 조명 켜려면 Lit 전환 필수
- `Light2D`가 Global 하나뿐 (`BaseScene.cs:480`), 노멀맵 없음
- 그림자는 런타임이 아니라 생성기가 스프라이트에 구워 넣은 것 (`tools/sprites/tiles.py:262`)
- 벽 타일이 32×32 평면 — 정면(높이)이 없음

**배치 1 — 워커 3개 병렬 (진행 중, 각자 워크트리/브랜치)**
| 워커 | 범위 | 소유 파일 |
|---|---|---|
| W1 | 소품 셰이딩(광원 좌상단 45°)·구운 그림자 제거·벽 정면 타일·노멀맵 산출 | `tools/sprites/**`, `Art/2d/{props,tiles}/**` |
| W2 | 벽 정면 배치+Y소트·런타임 캐스트 섀도우·바닥 AO | 신규 `Net/WorldDepth.cs`, `BaseScene.cs` 벽·소품 구역 |
| W3 | 노멀맵 연결·Lit 머티리얼 전환·Light2D 배치·주야 연동·성능 가드 | 신규 `Net/WorldLighting.cs`, `Sprite2DImport.cs`, `WeatherGrading.cs`, `BaseScene.cs` 조명 구역 |

**워커 간 계약 (확정 — 바꾸려면 셋 다 고쳐야 함)**
- 벽 정면 타일: `Art/2d/tiles/wall_{kind}_face.png`, **32×26px**, kind = interior/utility/outdoor/wood (fence 없음)
- 노멀맵: 원본과 같은 폴더 + `_n` 접미사, 원본과 같은 크기, 아틀라스 제외
- 그림자는 **런타임 담당**(W2). 생성기는 굽지 않는다(W1이 제거)
- 광원 방향 **좌상단 45°** 고정 — 아트·런타임 그림자 모두 같은 방향

**충돌 지점**: `BaseScene.cs`를 W2·W3가 함께 만짐 (다른 메서드 영역으로 분리 지시). 병합은 오케스트레이터.

**배치 1 결과 — 3워커 모두 회수·병합 완료** (통합 브랜치 `feature/depth-d`)
- W1 `art/w1-shading-normals` — 소품 105종 셰이딩·구운 그림자 제거·노멀맵 207장·벽 정면 4종. 결정성 2회 실행 483 PNG 해시 일치
- W2 `feature/w2-wall-depth-shadow-ao` — 벽 정면 1,076개·바닥 AO 6,995칸·런타임 그림자. 콜라이더 무변경
- W3 `worktree-agent-abea57310a93f5bfc` — Lit 전환·Light2D 104개(화면당 8 상한)·노멀맵 연결·아틀라스 배제·조명 OFF 토글

**리뷰에서 잡아 고친 것 (기록 — 같은 실수 반복 방지)**
1. *벽 정면 색 이탈*: `face ×1.4` 후 팔레트 **전체**에서 최근접 스냅 → 콘크리트 계열에 밝은 중성 톤이 없어 `grass`로 튐(정면이 이끼처럼 보임). W1 재작업 → 색을 계산하지 않고 `palette.neighbor()`로 **같은 계열 안에서 단만 올림**. 구조적으로 재발 불가
2. *조명 스윕 경계 3건* (오케스트레이터가 직접 수정, 커밋 "조명 스윕 경계 정리"):
   - 벽 정면은 타일맵이 아니라 낱개 스프라이트 → 스윕에 안 잡혀 **벽 위/아래 밝기가 어긋남** → `TM_WallFace` 명시 포함
   - 바닥 AO(`TM_FloorAO`)가 Lit이 되면 **등 아래에서 스스로 밝아져** 접지 그늘이 사라짐 → 제외
   - 소품 그림자(`WorldDepth.ShadowChildName` = "그림자")도 같은 이유로 제외
   - 컨테이너 이름을 상수화(`WallFaceLayerName`/`FloorAoLayerName`) — 이름 드리프트가 곧 회귀라서

**배포 완료 (8/5 11:2x)** — 씬 생성·WebGL 빌드 모두 `Exiting batchmode successfully`, 컴파일 오류 0.
씬 실측: 벽 정면 1,076 · 소품 그림자 493 · 로컬 광원 104 · AO 타일맵 1.
`Sprite-Lit-Default` 셰이더가 빌드 로그에서 실제 컴파일 확인됨(분홍 셰이더 위험 해소).
web.loader.js sha256 로컬=원격 `551ccaa6…` 일치. (첫 배포 시도는 `fetch failed`로 실패 → 재시도 성공)

**1차 배포 후 사용자 지적 → 수정 재배포 (같은 날)**
사용자가 화면을 보고 "그림자가 전방면에 다 떠 있다 / 벽 아래 이상한 띠". 씬을 캡처해 원인 특정:
1. *벽 무늬 중복*: **벽 타일(`tiles.wall()`)이 이미 제 아래 10px에 남쪽 면을 굽고 있다.**
   그 아래에 정면을 또 붙이니 "구운 남쪽 면 → 정면"이 연달아 나와 이음매가 밝은 띠로 보였다.
   (W4는 `flipY`로 톤을 맞췄는데 그러면 **바닥 접촉부가 가장 밝아져** 물리적으로 거꾸로 →
   오케스트레이터가 설계 교체: 정면이 그 10px를 **덮도록** 올리고(`WallBakedFaceHeight`),
   정면 색은 `top`에서 시작해 `face`로 내려오게 재작성. 밝히지 않으니 튈 색이 없다)
2. *AO 계단*: 타일 균일 알파(0.3)는 그라디언트가 될 수 없다 → 벽 가장자리 **그라디언트
   스프라이트**로 교체(W4), 강도 0.18. 소품 그림자는 반대로 0.32→0.42로 또렷하게.

검증(캡처 픽셀): 북쪽 벽 세로 단면이 `밝은 윗면 → 어두운 정면 → 바닥` 한 번씩만.
남쪽 바닥 AO는 18px 매끄러운 램프(126→120), 하드에지 없음.
재배포: `web.data.br` 8,454,956 → 8,671,666 바이트, 로컬=원격 해시 일치.
(주의: `web.loader.js` 해시는 아트·씬만 바뀌면 **안 변한다** — 내용 검증은 `web.data.br`로 해라)

**2차 사용자 지시 → 4건 처리·재배포 (같은 날 3차 배포)**
사용자 요구: ①남쪽 면 빼고 일원화해 입체감 살려 ②캐릭터도 조명 받게 ③그림자가 작은 원이고
에셋과 안 이어짐(좌상단 태양광 가정이 이상함) ④라이트는 어두울 때만, 자연스럽게 페이드
- **①** `tiles.wall()`에서 구운 남쪽 면 제거 → 벽은 윗면만. `wall_face()`를 32×26 → **32×32**
  4밴드(림/윗면톤/정면톤/접지선)로 재작성. 배치는 겹침 보정(`WallBakedFaceHeight`) 제거하고
  벽 칸 바로 아래로 원복. **주의**: utility/outdoor는 `face`가 `night0`(거의 검정)라
  1차 결과가 "검은 판때기"였다 → 아주 어두운 색은 몸통 금지, 접지선 3px에만. 판정은
  "정면 중간 밝기 ≥ 윗면의 55%"로 수치화(interior 73 / wood 75 / util·outdoor 100%)
- **②** `CharacterRig`가 런타임에 Lit 머티리얼 부여(노멀맵은 없어 밝기·색만 받는다).
  HUD는 전부 IMGUI라 영향 없음. 발밑 그림자는 렌더러 배열 밖이라 unlit 유지
- **③** 그림자를 **대상 알파 실루엣**에서 굽는 접지 그림자로 교체(고유 텍스처 98장, 파일 키 캐시).
  오프셋 0(실내 주광은 천장등). **추가 발견**: 그림자가 소품 밑변 *안쪽*에만 그려져
  소품에 가려 거의 안 보였다 → 바닥으로 번지게 수정, 농도 0.42→0.5
- **④** `WorldLighting`이 `NightAmount` 연동 + 목표값 하나로 합쳐 `MoveTowards`(0.4초) 페이드.
  세기가 0으로 수렴한 뒤에만 `enabled=false` → 튐 없음 + 드로우콜 절약 유지
- **덤(기존 버그)**: `CharacterRig._blinkAt`를 필드 초기화식에서 `Random.Range`로 뽑아
  `UnityException: Range is not allowed to be called from a MonoBehaviour constructor`가
  매 생성마다 났다 → `Awake`로 이동. 씬 로그의 UnityException **0건**. 눈 깜빡임 위상이
  실제로 흩어지는지는 사람 확인 필요

검증: 씬·빌드 `error CS` 0, `Exiting batchmode successfully`.
생활관·복도·외곽 3구역 캡처 확인 — 벽 단면이 `윗면 → 밝은 모서리 → 정면 → 접지선 → 바닥`
한 번씩만(픽셀 실측). 재배포 후 `web.data.br`·`web.wasm.br` 로컬=원격 일치.

**3차 지적 → 4차 배포**: "벽 아랫변이 막힌 것처럼 보이는데 통과된다 / 벽 아랫변 그림자가 듬성듬성"
→ **두 증상이 한 원인.** 정면을 32px(한 타일)로 만들어 벽 칸 *아래* 바닥 타일을 통째로 덮었다.
  - 콜라이더는 벽 한 칸인데 그림은 두 칸 → 보이는 벽과 실제 벽이 한 칸 어긋남
  - 그 바닥 칸의 AO가 정면에 가림. 정면이 그려지는 칸(남쪽이 바닥인 벽)과 아닌 칸이 섞여 듬성듬성
→ 수정: `WALL_FACE_H` 32 → **16(반 칸)**, 배치를 **벽 칸 안쪽**으로(`faceTop = wallBottomY + bounds.size.y`),
  Y소트 기준선 `splitY = wallBottomY`. 벽 한 칸 안에서 윗면·정면 두 평면이 다 보이고 콜라이더와 일치.
검증: 벽 아래 AO가 전 구간 연속(벽 바로 아래 100%, 12px에 걸쳐 바닥으로 수렴 — 직전엔 구멍이 섞임).
재배포 `web.data.br` 로컬=원격 일치.
**교훈(반복 금지)**: 탑다운 오블리크에서 **정면 높이는 콜라이더 폭을 넘으면 안 된다.**
더 높이려면 콜라이더를 벽 아래 한 칸까지 넓혀야 하고, 그건 맵 전체 통행 폭이 바뀌는 결정이다(사용자 판단 필요).

**5차 배포**: 사용자 요청으로 벽 밑 그늘(AO) 농도 `WorldDepth.AoAlpha` 0.18 → **0.30**.
예전에 0.3이 과했던 것은 값이 커서가 아니라 **타일 단위 균일 알파**라 계단이 보였기 때문 —
지금은 그라디언트라 같은 값이 자연스럽다. 벽 접촉부 바닥 밝기 147 → 139(캡처 실측).
농도 조절은 이 상수 하나만 바꾸고 씬 재생성하면 된다(0이면 AO 자체가 꺼진다).

## 대기 중인 지시 (8/7 밤 — 미완)

**진행 중**
- **H1** (워커 실행 중): 위병소를 정문에 겹치게 배치(밖으로 나가려면 위병소 통과) ·
  위병소~훈련장 `asphalt` 길 · 그 길이 미니맵에 보이게. 소유: `basemap.py`·`trainmap.py`·`Hud.cs` 미니맵부

**대기(H1 병합 후 착수 — Hud.cs 미니맵부가 겹친다)**
- **퀵 커맨드를 미니맵에 표시**: 사용자 — "퀵 커맨드는 미니맵에도 표시되게 해야지, 다른 인원이 볼 수 있게".
  현재 `Q(홀드)+1~8` 라디얼(`HudScreens.cs:130-136`)은 발신자 화면에만 뜬다. **분대원 전원의 미니맵에**
  전술 지시 마커가 보여야 한다 → 서버 경유가 필요할 수 있다(프로토콜 확인 필요.
  `packages/protocol`에 퀵커맨드 인텐트가 있는지부터 볼 것). 있으면 클라만, 없으면 sim+protocol+push까지.

**대기(발주 예정)**
- **온보딩 전면 교체**: 사용자 — "온보딩 그냥 저따구로 하지 말고, 그냥 로비에서 튜토리얼 누르면
  하나하나 알려주는 걸로 바꿔". 즉 **게임 내 8단계 카드를 걷어내고 로비(Next.js)에 튜토리얼 페이지**를 둔다.
- **조작키 창을 `F1` → `1` 키로**: "실제 게임에서 조작키 창 뜨는 건 1번 키 눌렀을 때".
  ⚠ `1`은 이미 `Q(홀드)+1~8` 퀵커맨드와 `C(홀드)+1~8` 정형문구에서 쓰인다 — **홀드 조합이 아닐 때만**
  조작키 창이 뜨도록 해야 충돌하지 않는다. 이 점을 발주에 반드시 포함할 것.

## 정문을 뚫었다 — 훈련장 도달 불가 (8/7 — 배포 완료)

사용자: "훈련장으로 넘어갈 수가 없는데?" → **맞다. 훈련장 10곳 전부 걸어서 도달 불가였다.**

원인은 주석과 코드의 불일치다. `basemap.py`가 외곽 철조망을 두르며 바로 위에
> "외곽 철조망 — **부대만** 두른다. 훈련장은 그 밖이고, **정문(Z18)으로 나가서 간다**"

라고 적어 놓고 **정문 구멍을 안 뚫었다.** `walls.outline(1,1,BASE_W-2,BASE_H-2,"wall:fence")`가
부대를 빈틈없이 둘러싸 밖으로 나갈 칸이 하나도 없었다.

플러드필(생활관 스폰 기준): 도달 불가 **10곳 → 0곳**
(사격장·화생방 제독소·숙영지·혹한기 훈련장·혹서기 급수 라인·대민지원 마을·합동 전술훈련장·행군로·유격장 기초/종합)

수정: 위병소 Z18 가운데 x52~56에 **5칸 폭 정문**(4인이 함께 지나갈 폭). `protected`에도 넣어
남향 벽 두껍게 같은 후처리가 다시 막지 않게 했다.

**왜 심각했나**: `curriculum.json`이 D-03부터 훈련을 배정한다(사격훈련 → 사격장). 즉 3일차부터
**갈 수 없는 곳으로 가라는 필수 일과**가 나오고, 필수를 못 채우면 점호에서 깨진다 — 사실상 D-03 진행 불가.

**교훈**: C1(벽 2타일) 워커가 이걸 "C1과 무관한 기존 문제"로 정확히 보고했고 HANDOFF에도 적었는데,
별건이라 미뤄 두는 사이 실제로 게임을 막고 있었다. **연결성 플러드필은 맵 생성기의 상시 검사로 넣을 가치가 있다**(미착수).

검증: 결정성 2회 실행 해시 동일 · 팔레트 통과 · 씬/빌드 `error CS` 0 · `web.data.br` 로컬=원격 일치.

## 계급 표시 · 구제권 · 튜토리얼 키 (8/7 — 배포 완료)

**⚠ 이 배치의 핵심: 구제권이 18일 내내 발동 불가였다 (죽은 기능)**
- `relief.ts::canUseRelief` 첫 줄이 `state.leaderId !== leaderId`면 거부
- 그런데 **`state.leaderId`가 sim 어디에서도 할당되지 않았다** — `run.ts`에서 `null`로 시작하고 끝
- `packages/protocol`에 `voteLeader` 인텐트가 있는데 **sim이 처리하지 않았다**(`grep` 0건)
- 화면엔 "구제권 3장"이 뜨는데 눌러도 조용히 거부. 에러도 안 나서 발견이 늦었다 — **사용자가 직접 플레이해 잡았다**
- 더 나쁜 것: `room.ts`의 옛 `voteLeader`가 **`step()`을 우회해 `this.run.leaderId`를 직접 변경**했다.
  서버 권위(ARCH-02)를 깨고 리플레이를 망가뜨리는 코드였다 → 인텐트 경유로 정상화
- **기존 테스트가 이 버그를 정답으로 박아 두고 있었다**(2/4 투표 후 `leaderId`는 `null`이라고 단정) → 재작성

수정: 신규 `packages/sim/src/leader.ts` — `pickLeader`(런 시작 시 사람 참석자 중 ROLES 순서로 **결정적**
선택, 1인 방 포함) · `reassignLeaderIfVacant`(이탈·후송 시 재지정) · `voteLeader`(과반, 동수면 현직 유지,
떠난 사람 표는 분모에서도 제외). `SAVE_VERSION` 3→4(새 필수 필드) — **진행 중 저장은 이어하기 불가**.
구제 버튼·거부 사유 7종은 **이미 구현돼 있었다** — `leaderId`가 null이라 한 번도 발화하지 못했을 뿐.
분대장 다이아몬드 마커도 같은 이유로 죽어 있다가 살아났다.

**계급 표시**: 머리 위 이름표가 `이름 계급 · 보직`으로. 진급 시 일과요약 탭에 "진급! 이병 → 일병" 배너
(그날 하루). 기존 `rankReviewed` 이펙트 재사용 — 새 이펙트를 만들지 않았다.

**튜토리얼 키 안내**: `F1` 전체 조작표(첫 이동 단계에서 1회 자동) + 단계 카드에 키캡.
워커가 **파일:줄 출처를 붙인 키 전수 조사표**를 냈고, 그 과정에서 **아무도 안 알려주던 키들**이 드러났다 —
`SHIFT` 달리기 · `M` 지도 · `Q+1~8` 퀵커맨드 · `C+1~8` 정형문구 · `ALT` 컨디션 · `G` 방독면.
시간대 스킵은 키가 아니라 마우스 클릭이다.

**밸런스 테스트 하나 손봄**: `구제권을 늘리면 완주율이 급격히 오른다`가 5초 제한에 타임아웃.
단정이 틀린 게 아니라 **구제가 실제로 발동되며 런이 오래 살아남아** 시뮬이 길어진 것. 이웃 테스트와 같은 30초로.

검증: typecheck 0 · 테스트 519개 전량 통과(sim 419 + 신규 `leader.test.ts` 13건) ·
씬/빌드 `error CS` 0 · `web.data.br` 로컬=원격 일치 · **push로 Render 재배포·`/health` 정상** ·
브라우저 실플레이로 **이름표에 계급(이병) 표시 확인**.

**미검증**: 구제권이 실제로 눌리는 장면(분대장 + 저녁 + 미달 필수가 동시에 필요해 짧은 세션으로는 재현 곤란).
유닛 테스트에는 "자동 지정된 분대장으로 구제가 실제 발동된다"가 들어 있다.

## 온보딩 튜토리얼 (8/7 — 배포 완료)

**이미 있던 것과 없던 것을 먼저 갈랐다.** `curriculum.json`이 날짜별 점진 해금을 이미 한다
(D-01 기본조작·수첩 / D-02 보직퀘·공통일과·군기 / D-03 보급·무전·계급·하달).
즉 "언제 무엇이 열리는가"는 설계돼 있었고, 없던 것은 **"어떻게 조작하는가"**였다. 그것만 만들었다.

**설계**: 첫 런 D-01에 뜨는 8단계 카드(`unity/Assets/Scripts/Net/Tutorial.cs` 신규).
별도 튜토리얼 맵을 만들지 않고 실제 게임 안에서 가르친다.
- **클라이언트 전용** — 스냅샷(`GameClient.Latest`)과 입력만 관찰. 서버·프로토콜 무변경이라 Vercel 배포만으로 끝난다
- **막지 않는다** — 8개 완료 플래그를 매 프레임 독립 검사, 순서를 안 지켜도 완료되면 넘어간다.
  카드는 "아직 안 끝난 것 중 가장 앞선 것" 하나만
- 다른 전체화면(하달·마감 큐·미니게임·통합 수첩·QTE·컷인)이 뜨면 물러난다. `day > 1`이면 종료
- ESC/버튼으로 건너뛰기 · `PlayerPrefs`에 저장 · 웹 설정에 "튜토리얼 다시 보기"

| # | 단계 | 완료 판정 |
|---|---|---|
| 1 | 이동 | 이동 입력 |
| 2 | 일과표 | TAB |
| 3 | 목표 찾기 | `Interactor.Nearest != null` |
| 4 | 상호작용 | `QuestPlay.QuestId != null` |
| 5 | 미니게임 | 자기 퀘스트가 스냅샷에서 `done` |
| 6 | 필수·선택 | TAB 2회째(일과요약 탭) — 순수 설명 단계라 대체 관찰 |
| 7 | 시간대 | `phase.id` 변화 |
| 8 | 점호·마감 | `phase.id == "rollcall"` |

**브라우저 실플레이로 검증 완료**: 1/8 카드 표시 → 이동하니 2/8로 전환 → ESC로 사라짐 →
**새로고침 후 다시 안 뜸**. 워커가 "미검증"으로 남긴 **`PlayerPrefs`의 WebGL 영속화가 실제로 됨을 확인**했다.

**남은 것**: F10 ScreenLab 프리셋 미추가 — `ScreenLab.cs`가 오케스트레이터가 금지한
`Minigame/**` 경로 안에 있어 워커가 지시를 지켰다(발주 경계가 과했다). 검증 편의용으로 후속.

## UI/월드 배치 E (8/6~7 — 배포 완료)

사용자 지시 10건. `Hud.cs`+`HudScreens.cs` / `HudIcons.cs`+`TraceBoard.cs` / `BaseScene.cs`로
파일 소유를 갈라 3병렬. 서버(`packages/sim`)도 바뀌어 **Vercel + `git push` 둘 다** 했다.

**E1 수첩·일과표 통합** (`Screen.Schedule` 제거 → `Screen.Notebook` + `NotebookTab` 3종)
- TAB 순환 닫힘→일과표→일과요약→기록→닫힘, ESC 언제든 닫힘. 내브바 64px를 **창 위쪽**에 크게
- 하루 시작 시 일과표 자동(기존 날짜변경 감지 재사용, 하달 창 우선순위 유지)
- 기록 정렬 `Insert(0)` → `Add`(먼저 한 것이 위), 200줄 상한도 오래된 것부터
- 기록 초기화를 `ClearJournalForNewDay` 하나로 통합 — **비우는 시점 = 다음 날 실제 시작**.
  마감 확인 게이트 때문에 날짜가 [확인] 후에 오르므로 판정 화면이 그날 기록을 읽을 수 있어야 한다
- 점호 판정 패널 152px 위로(452→300), 창 위에 보직 4종 인형 + 실패자 표시.
  **조건 A·D는 특정 인원 지목, B·C는 분대 공통이라 지목 없음**(판정 규칙상 맞다)
- **한계**: 캐릭터가 실제 스프라이트가 아니라 색 인형이다. `CharacterRig` 레이어가 전부 private이라
  HUD에서 접근 불가 — 목업(MOCKUP_06)의 `#stand`도 사각형 인형이라 목업과는 일치한다.
  진짜 스프라이트로 가려면 `CharacterRig`에 접근자를 여는 후속 작업

**E2 마커·TRACE**
- 마커 1.35배 + `HudTheme.Paper` 테두리 2px + 채도/명도 보정(색조 유지). 소총 `6E7A50`→`859854` 등.
  **보직별 형태(원·사각·십자·삼각) 유지** — 색각 접근성 장치다
- TRACE 합동에서 조작 역할에게 관 방향·연결색·물(정답)을 안 그린다. 상태 텍스트도 막음
- **한계**: 정답은 서버가 역할별로 나눠 보내는 게 아니라 두 클라가 **같은 시드로 각자 로컬 생성**한다.
  즉 조작자 메모리에도 정답이 있고 이번 수정은 "안 그리기"뿐이다(watch 역할도 원래 같은 한계).
  진짜 차단은 프로토콜 변경이 필요

**E3 간부 NPC 4명** — 무기고(소총) Z09 · 통신실(통신) Z06 · 의무실(의무) Z05 · 행정반(행정) Z04.
계급 `sergeant`, idle만, 콜라이더 없음.
- **잠복 버그 발견·수정**: `CharacterRig._library`/`_clip`이 직렬화되지 않아 **씬에 미리 구운 캐릭터는
  실제 게임에서 투명하게 나온다.** 플레이어·분대원은 런타임에 새로 만들어져 무사했지만 간부는
  아무도 다시 입혀주지 않는다 → 신규 `OfficerNpc.cs`가 `Start()`에서 스스로 재장착

**무전 두절 관련 (사용자 질문 → 버그 하나 발견)**
질문: "통신병이 NPC면? 첫날 아침엔 살아있다가 오전 일과 시작하면 두절 뜨던데"
- NPC 통신병이 `down`인 것은 **설계가 맞다**(공석·이탈 대리는 유지할 사람이 없음. `npcEvac`만 `weak`)
- 그런데 `run.ts`가 `radio: "ok"`를 하드코딩하고 "첫 tick에서 갱신된다"고 주석을 달아 뒀는데
  **틀렸다** — `refreshRadio`는 `endPhase`(시간대 경계)에서만 돈다(매 틱 퀘스트 배열을 훑으면
  배치 시뮬이 수백만 번 순회해서 일부러 그렇게 뒀다). 그래서 첫 시간대 내내 살아 있는 척했다
- → `createRun` 끝에서 `evaluateRadio(state)`로 한 번 제대로 잰다. 회귀 테스트 2건 추가

**⚠ 캡처 함정 하나 더**: `CaptureScene`은 `Awake/Start`를 돌리지 않아 **`OfficerNpc`가 스프라이트를
재장착하기 전 상태**로 찍힌다 — 간부가 안 보인다고 버그로 오해하지 마라(조명 `-bakeLights`와 같은 종류).

검증: typecheck 0 · 테스트 406(sim)+전 워크스페이스 통과 · 씬/빌드 `error CS` 0 ·
`web.data.br` 로컬=원격 일치 · **브라우저에서 실제 플레이로 통합 창 3탭·기록 정렬 육안 확인**.

## 하루 마감 확인 게이트 (8/5 밤 — 배포 완료)

**사용자 신고 2건**: "점호 판정 확인 누르면 **다음날** 요약이 뜬다(D-1 끝났는데 D-2 요약)" ·
"요약창 확인 전까지 시간이 가면 안 되는데 **간다**".

**원인은 하나**: `step.ts`의 `endDay()`가 판정→승급→취침정산→`state.day += 1`→`beginDay()`까지
**한 번의 동기 호출**로 끝냈다. 서버가 [확인]을 기다리지 않으니
① 요약 화면이 읽는 스냅샷이 이미 `day+1`이고 ② 타이머가 계속 흐른다.

**해법 — 새로 발명하지 않고 하달 창(QST-04) 선례를 그대로 따랐다**
(`delegationWindowMsLeft > 0`이면 `applyTick`이 시간대 타이머를 소비하지 않는 그 구조):
- `endDay()` → `settleDay()`(판정~취침정산, **날짜 안 올림**, 끝나면 마감 창 열기) +
  `finishDay()`(`day += 1` ~ `beginDay()`)로 분리
- `dayEndWindowMsLeft`가 도는 동안 시간대 타이머 정지. 초과분(leftover)은 다음 날로 이월
- 새 인텐트 `dayEndAck` — 사람 참석자(`presence === "player"`) 전원이 보내야 즉시 `finishDay()`
- **백스톱 `DAY_END_BACKSTOP_MS = 90_000`** — 한 명이 방치/끊기면 방 전체가 영원히 멈춘다
- 접속 종료 시 서버가 대신 확인 처리(하달 창 disconnect 처리와 같은 패턴)
- 클라(`HudScreens.cs`)는 마감 큐 마지막 화면이 닫힐 때 전송. **날짜를 클라에서 보정하는 우회는 금지**했다 —
  스냅샷이 아직 D이므로 요약이 저절로 맞는다

검증: typecheck 전 워크스페이스 0 에러 · `npm run test --workspaces` **497개 전량 통과**
(신규 12건: 창이 날짜·타이머를 멈춤 / 전원 확인 시 통과 / NPC 제외 / 백스톱 자동 통과 / 이월 /
창 중 이탈 시 대기 제외 / 인텐트 전달 / tick만으로는 안 넘어감) · 씬·빌드 `error CS` 0.

**⚠ 이번에 처음 겪은 배포 함정 — 반드시 기억할 것**
이 저장소는 **웹(Vercel)과 게임서버(Render)가 따로 배포된다.**
- `npx vercel --prod --yes` → 웹/Unity 빌드만
- **`git push` → Render 게임서버**
아트·씬만 바뀔 때는 Vercel만으로 충분해서 그동안 문제가 없었는데, 이번처럼
`packages/sim`·`packages/protocol`·`services/gameserver`가 바뀌면 **push를 빼먹는 순간
새 클라 + 옛 서버 조합**이 되어 수정이 아무 효과가 없다(클라가 보낸 `dayEndAck`를 서버가 모른다).
이번에 로컬이 origin보다 **56커밋** 앞서 있었다 → 푸시 후 서버 재시작·`/health` 정상 확인.

**원격 서버가 새 코드인지 직접 증명하지는 못했다**: `PROTOCOL_VERSION`을 안 올렸고(추가만 한 변경),
서버가 모르는 인텐트를 조용히 무시해서 판별자가 없다. 정황(푸시 직후 재시작)만 있다.
→ **다음에 프로토콜을 바꾸면 `PROTOCOL_VERSION`을 올려라.** 그러면 `welcome` 메시지로 바로 판별된다.

## 소품 조각 합성 (8/5 21시 — 배포 완료)

**계기**: 사용자 — "맵에 있는 소품들 좀 고퀄리티로 뽑아주면 안 돼? 캐릭터처럼 여러 매쉬 뽑아서 합쳐주면 되잖아."

**진단**: 소품 105종 중 26종이 `tiles._box()` 하나를 공유했는데 그 함수는
`PX.rect(...)` 한 줄, **단색 사각형 채우기가 전부**였다. 그래서 관물대도 서류함도
식탁도 "색만 다른 네모"였다.

**구조** (다음에 소품을 늘릴 때 이 배선을 쓴다)
- `tools/sprites/parts.py` — **조각 어휘**(공유). `slab` 몸통 · `grain(img,kind,key,seed)`
  재질결(wood/metal/fabric/mesh) · `panelize` 문·서랍 · `seam` · `handle`(bar/knob/latch) ·
  `vent` · `rim` 윗변 · `feet` 다리 · `label` 이름표 · `wear` 사용감
- `props_furniture.py` / `props_utility.py` / `props_outdoor.py` — 가족별 그림.
  각 모듈이 `BUILDERS`(이름→`f(w,h)->Image`)를 내보내면 `tiles.py`가 **그리는 함수만**
  갈아 끼운다. **타일 크기(w,h)는 절대 안 바꾼다** — `base_map.json`이 그 크기로 배치돼 있다
- 모듈을 셋으로 나눈 이유: 세 워커가 같은 파일을 동시에 고치면 충돌한다. 병합은 한 곳에서.

**결과**: 105종 **전부**(100%) 조각 합성. 차량=차체+바퀴+적재함, 차단기=줄무늬 팔+경광등,
게시판=압정 꽂힌 종이, 세탁기=둥근 문, 약품장=적십자, 보일러=배관+바늘 달린 압력계 등.

**⚠ 이 배치에서 잡은 함정 (재발 주의)**
`P.neighbor(key, step)`은 계열 끝에서 **자기 자신을 돌려준다(클램프)**. 그래서
`wood2`·`metal2`·`conc3`·`olive2`처럼 **이미 제 계열에서 가장 어두운 색**을 몸통으로 쓰면
이음매·통풍살·다리·결이 전부 "같은 색으로 칠하기" = **보이지 않는 no-op**가 된다.
에러도 안 나고 조용히 사라져서 발견이 늦다 — 실제로 집합 좌석이 옛 `_box()`와 똑같은
민무늬로 나왔고 서류함 서랍선·총기 거치대 칸막이가 사라져 있었다.
→ `parts._shade_key()`가 **계열 끝에서 반대 방향으로 접도록** 고쳤다(중앙 수정).
한 워커만 자기 파일에서 우회했었기 때문에, 어휘를 고친 뒤 **전체를 재생성**해야 나머지에도 반영된다.

**병합 관례**: 아트 PNG는 충돌해도 붙잡고 있지 마라 — **생성기가 진실**이다.
충돌을 임시 해소한 뒤 `generate.py`로 통째로 재생성하면 어느 워커 산출물이 남았는지가 무의미해진다.

검증: 결정성 2회 483장 해시 동일 · 팔레트 검사 통과 · 타일 크기 105종 불변 ·
씬/빌드 `error CS` 0 · 배포 후 `web.data.br` 로컬=원격 `5dbf331a…` 일치.

## 전수 감사 (8/5 18시~ 진행 중)

**계기**: 사용자 플레이 스크린샷(18:00)에서 "벽도 그림자도 그대로". 픽셀 분석 결과
**아트·조명·AO·벽 정면은 실제로 동작 중**이었다(아래 단면 참조). 진짜 문제는 따로 있었다 —
**캐릭터가 벽 영역 안에 그려져 있다**(그림 ↔ 충돌 불일치).

스크린샷 세로 단면(원본 2540×1578, x=762):
```
y   0..215  흙(172,151,109 → 161,137,101)  ← 벽 쪽으로 어두워짐 = AO 정상
y 216..375  (103,103,66) conc3             ← 벽 윗면(outdoor/utility는 top=conc3)
y 376..390  (140,139,101) conc2            ← 벽 정면 밝은 림
y 396..495  (103,103,66) conc3             ← 벽 정면 몸통
y 511..535  (24,31,46) night               ← 접지선
y 536+      (205,211,209)                  ← 실내 타일 바닥
```
**캐릭터 스프라이트가 y≈292~368(벽 윗면 구간)에 그려짐.**
배포 시각 13:59 < 스크린샷 18:00, 원격 해시 = 로컬 → 캐시 문제 아님. 최신 빌드가 맞다.

**중요**: 화면 배율을 스크린샷만으로 확정하지 못했다(캐릭터 높이와 타일 크기가 안 맞음).
그래서 **엔진에서 재현**하는 것이 B1의 첫 임무다 — 스크린샷 눈대중으로 고치지 말 것.

**워커 3개 병렬 (각자 워크트리)**
| 워커 | 범위 |
|---|---|
| B1 | **벽 그림↔충돌 불일치** 재현·원인·수정. 서버 권위(ARCH-02)라 서버 충돌 데이터와 `base_map.json` 대조 포함 |
| B2 | 실제 렌더 전수 점검(구역별 벽 5종·AO 연속성·소품 그림자·광원·회귀). 취향 문제는 수정 금지, 보고만 |
| B3 | 런타임 오류·경고 사냥(필드 초기화식 엔진 API, null, sortingOrder 16비트, Shader.Find, 매 프레임 비싼 호출) + 기존 실패 테스트 2건 수리 |

파일 소유 분리: B1=충돌·이동, B2=렌더·캡처, B3=그 외 스크립트·테스트.
공통 금지: `tools/sprites/**`·`Art/**`는 이번 배치에서 아무도 안 건드린다.

**감사 결과 (B1·B2·B3 회수 완료)**

- **B3** — typecheck를 통째로 막던 `packages/sim/src/quests.ts` TS2352 수정(4개 워크스페이스 0 에러),
  오래 빨갛던 테스트 2건 그린. `wire.test.ts` 실패 원인은 코드가 아니라 **8099를 2일간 점유한 고아
  프로세스**(`node tools/m0serve/serve.mjs .../hd2d 8099`)였다 → 정리 후 52/52 통과.
- **B1** — 스테일 워크트리에서 분기돼 대상 코드가 없었다(발주 실수). 다만 유용한 검증은 남겼다:
  **클라 콜라이더가 맵과 셀 단위로 일치**, 36개 구역 스폰 전부 바닥 위, 벽 위 스폰 0건.
- **오케스트레이터가 배포본을 직접 플레이해 확인** — 벽 충돌 정상(W 60회에 0px 이동),
  접지 그림자·벽 정면 모두 화면에 나옴. **서버에는 벽·통행 데이터가 아예 없다**(이동은 전적으로 클라 물리).

**⚠ B2의 헤드라인은 틀렸다 — 반드시 기록해 둔다**
B2가 "로컬 광원 104개가 렌더에 전혀 기여하지 않는다"고 보고했다(ON/OFF 캡처 픽셀 동일).
**원인은 게임이 아니라 캡처 도구였다.** URP `Light2D`는 `UpdateMesh()`를 **`LateUpdate`에서만**
부른다(패키지 `Light2D.cs`). 배치모드는 플레이 모드가 아니라 `LateUpdate`가 돌지 않아
**Point 광원 메시가 아예 생성되지 않는다**(씬 YAML에서 Point 광원 `m_LocalBounds.m_Extent`가 전부 0,
Global만 0.998로 정상 — 이게 증거다). 리플렉션으로 `UpdateMesh(true)`를 강제한 뒤 다시 찍으니
ON/OFF 평균차 **83.1**, 최대차 282로 확연히 갈렸다. **로컬 조명은 런타임에서 정상 동작한다.**
→ `CaptureScene`에 `-bakeLights` 플래그를 넣어 이 함정을 영구히 막았다. **앞으로 배치 캡처로
조명을 판단할 때는 반드시 `-bakeLights`를 붙여라.** 안 붙이면 조명이 없는 것처럼 보인다.

**그래서 사용자가 조명을 못 본 진짜 이유**: 낮이다. 사용자 요청("어두워졌을 때만 뜨게 해")대로
`NightAmount` 연동이라 낮에는 실내등이 꺼진다. 1일차 오전·오후만 플레이하면 조명 작업이 통째로 안 보인다.
→ **후속 판단 필요**: 실내(지붕 있는 방)는 낮에도 바깥보다 어둡게 깔아야 등이 의미를 갖는다.

**벽이 "그대로"로 보이는 이유(확정)**: 벽 전체가 1타일인데 캐릭터도 1타일(32px)이라,
벽 앞에 서면 몸이 벽 높이를 통째로 가린다. 통과가 아니라 **가림**이다.
정면을 칸 밖으로 내리면 "막힌 줄 알았는데 통과"가 되고, 칸 안에 넣으면 "캐릭터가 벽을 덮는다"가 된다 —
**같은 뿌리**다. 해결하려면 벽이 2타일을 차지하고 둘 다 막혀야 하며, 그건 통행 폭이 줄어드는
게임 밸런스 결정이라 사용자 승인 필요.

**추가 발견**: 동서(수직) 벽은 정면이 아예 없어 납작하다(남향 벽만 정면 생성). `fence`(맵에서 2번째로
흔한 벽, 626칸)도 정면 자산이 없어 조용히 건너뛴다.

**사용자 승인 후 2건 실행 (C1·C2) — 병합 완료, 배포 대기**

**C2 실내 낮 어둡게** (`c2-indoor-dim-daylight`)
- `WeatherGrading.DarknessAmount = Max(_night, _indoor * IndoorDarkAmount)` — **곱하지 않고 Max**.
  곱했으면 밤+실내가 겹쳐 이중 감쇠로 방이 죽는다. Max면 밤에는 실내 여부와 무관하게
  순수 야간 값이라 **수식상 이중 감쇠 불가**.
- `WorldLighting`이 실내등 점등 판정에 `NightAmount` 대신 `DarknessAmount`를 본다 → 낮에도 실내면 켜짐
- `IndoorDarkAmount = 0` 이면 기능 완전 OFF(롤백 안전장치), `IndoorFadeSeconds = 0.5`
- **오케스트레이터 수정 2건**:
  1. 워커가 실내 어두움에 밤의 청색조를 같이 썼다 → 대낮 실내가 푸르게 뜬다.
     **색 Lerp는 `_night`만, 밝기 Lerp는 `DarknessAmount`**로 분리.
     검증(청색편차): OFF −31.1 / 실내낮 −28.9(거의 불변) / 밤 −12.3(의도대로)
  2. 색 이동을 빼니 체감이 약해져 강도 재측정 — **평균 밝기가 아니라 명암 폭**이 판정 기준이다
     (등이 만든 밝은 자리는 두고 구석만 떨어져야 "등이 켜졌다"로 읽힘):
     0.55 → 104.7 / 0.75 → 111.0 / 0.9 → 115.3 (OFF는 89.0) → **0.8 채택**
- `CaptureScene`에 `-simulateDarkness`/`-simulateNight` 추가(배치모드는 `WeatherGrading.Update`가 안 돈다)

**C1 남향 벽 2타일** (`c1-south-wall-thicken`)
- `basemap.py`에 `_thicken_south_walls()` — 남쪽이 바닥인 벽 칸의 그 남쪽 칸을 같은 종류 벽으로 승격.
  단일 패스(스냅샷 기준)라 연쇄 승격 없음
- **발주서에 없던 함정을 워커가 찾음**: `doors` 목록만으로는 부족하다 —
  철조망 출입구·훈련장 입구는 `doors`에 아예 없고, 보일러실 연결 통로는 두 칸 중 한 칸만 기록돼 있다.
  벽을 뚫는 **모든 코드 경로**(`_outdoor`/`_building`/`_connect_boiler_to_washroom`/`_training`)에서
  뚫린 칸을 `protected`로 수집해 제외. 소품 칸·스폰 칸·1칸 틈도 제외
- 결과: 벽 2,410 → 3,667칸(+1,257). 바닥 칸 수·문 21·소품 493·구역 36 전부 불변
- 검증: 연결성 플러드필 26개 구역 전부 상호 도달 / 문 21개 양쪽 같은 영역 / 소품·스폰 벽 침범 0건 /
  결정성 2회 실행 바이트 동일

**이번 배치에서 드러난 기존 문제(이번 변경과 무관, 별건)**
- **훈련장 TR* 10개 구역이 걸어서 도달 불가** — 부대 외곽 철조망에 출입구가 한 개도 없다.
  C1 이전 커밋에서도 같음을 워커가 `git stash`로 대조 확인. HANDOFF에는 "텔레포트 없이 훈련장까지
  걸어가야 함"이라고 적혀 있는데 둘 중 하나가 틀렸다 — 구역 이동이 서버 주도 텔레포트인지 확인 필요

**배포 상태**: 씬 재생성 완료(`error CS` 0), WebGL 빌드 진행 중(20:26 시점 IL2CPP/WASM 단계).
빌드 후 webgl-sync → 배포 → **`web.data.br`** 해시 대조(loader.js는 아트·씬만 바뀌면 안 변한다).

이어가기 예약: 8/5 20:23 (발화 완료)

**남은 것**: 캐릭터 그림자는 아직 실루엣이 아니라 타원(런타임 생성이라 미처리) ·
utility/outdoor 벽은 팔레트 계열 끝이라 정면을 더 어둡게 할 색이 없어 입체감이 약함 ·
창 광원이 밤에 켜짐(논리상 반대, 맵에 창 1개뿐이라 보류)

**남은 트레이드오프(구 기록)**: 벽 입체감이 목업보다 약하다. 목업은 정면을 벽보다 **밝게** 칠했는데
실제 벽엔 어두운 남쪽 면이 구워져 있어 밝히면 반드시 띠가 생긴다. 더 강하게 하려면
벽 오토타일 80장에서 구운 남쪽 면을 빼고 정면 스프라이트로 일원화해야 한다(미착수).

**사람이 눈으로 확인할 것 (미검증 — 강력 새로고침 ⌘⇧R 후)**
- [ ] 실내에 들어가면 **등 아래가 밝고 구석이 어둡다** — 방마다 광원이 켜져 있는가
- [ ] 소품·캐릭터 **발밑에 그림자**가 생겨 바닥에 붙어 보이는가 (그림자 방향은 전부 우하단으로 같아야 정상)
- [ ] 벽 아래에 **밝은 정면 띠**가 생겨 방이 상자로 읽히는가 / 그 색이 벽과 같은 재질로 보이는가(초록으로 뜨면 회귀 — 보고)
- [ ] 벽 근처 바닥이 은은히 어두운가(AO). 과하면 `WorldDepth.AoAlpha` 한 값만 낮추면 된다
- [ ] **밤에 실내가 검게 죽지 않는가** / 낮에 등이 이상하게 튀지 않는가
- [ ] HUD·수첩·미니게임 판이 조명 영향을 **안 받는가** (어두워지면 즉시 회귀 — 보고)
- [ ] 걷는 판정·충돌이 예전과 같은가 (벽 정면은 시각 전용, 콜라이더 무변경)
- [ ] fps 체감 — 느려지면 조명 OFF 토글로 비교해 보고

**보류(배치 2 후보)**
- **캐릭터가 unlit로 남음** — 월드는 Lit인데 캐릭터만 안 받아 어두운 방에서 혼자 밝다. W3 소유 밖이라 미처리. 가장 눈에 띌 잔여 항목
- 캐릭터 노멀맵(`chars.py`는 이번 산출 대상이 아니었음)
- WebGL fps 실측 후 광원 수 상한 조정 · Shadow Caster 2D 도입 여부
- 창 광원은 맵에 `감시창` 1건뿐 — 창 데이터 자체가 빈약

이어가기 예약: 8/5 14:17 (세션 메모리에만 존재 — 창 유지 필요)


## 지금 돌아가는 것

| | 주소 |
|---|---|
| 웹 | https://solider-a-day.vercel.app — Vercel |
| 게임 서버 | https://sad-gameserver.onrender.com — Render 무료 (15분 슬립 → 로비 진입 시 /health 핑으로 깨움, 이번 세션에 넣음) |
| 저장소 | Upstash Redis + Supabase |

배포 검증: 로컬·원격 `web.loader.js` sha256 일치 확인함. 이 저장소는 "코드는 고쳤는데
화면은 옛것" 사고가 반복됐으므로 **배포마다 해시 대조가 관례다.**


## 이 세션에서 한 일 (요약 — 상세는 git log와 WORKORDER)

**문서 둘이 이 세션의 뼈대다:**
- `docs/BENCHMARK.md` — 리서치 16건 종합(포켓몬·동숲·좀비고·아이작·시장·아트 4종·소리·내러티브·온보딩·기술·침묵감사·현지화·확산). 결론: "없는 것은 아이디어가 아니라 마감"
- `docs/WORKORDER.md` — A~H 발주서. 모든 발주에 목표·완료판정·검증·의존·중단기준. **다음 세션은 여기서 집는다**

**배포된 것 (시간순):**
1. HUD 수리 묶음 — 복도 미니맵 프레이밍·지도 범례·배치도 방 이름·미니맵+수첩 붙이기
2. 하루 마감 화면 큐(점호 판정이 안 보이던 버그)·하달 창 가드·"이 방에서 다시" 수리·재접속 시 스냅샷 유실 수리·보일러실↔세면장 문
3. 구제권 표시(판정 화면) — 그리고 설계 어긋남 발견: 기획서는 발동형, 코드는 자동 카운터 (WORKORDER B-4)
4. A단계 배치: **F9 미니게임 실험대**(+`?lab=1` URL 진입, 첫 입력 전 시간 정지) · 소리 4종+플래시 베이스 승격 · 봇 수리(balance 6/6 사상 첫 그린 — sim은 무죄, 봇 회복 타이밍이 범인) · **재접속 백오프**(끊기면 4회 재시도+배너) · 해금 실시간 게이트(15/30/40분, 스킵 투표 면역)
5. 하달 창 조기 종료(전원 확정 시) + "하달 대기 — 시간 정지 (N초)" 표시 + 서버 틱 방탄
6. 실험대 피드백으로 잡은 버그: SORT 정답-이름 무관 버그·죽은 "4번" 통·civilaid 변형 누락 / HOLD 열자마자 이탈 누적 / 판 열려 있을 때 Space가 일과표 토글하던 것
7. 마지막 배치(방금 배포): **① 게임이 말한다**(침묵 토스트 12종·하달 말풍선·거부 사유 9종 부활·구제권 상시 표시·퇴소 화면 소대장 대사·UI배율 실동작·색각 마커) · **② 아트**(발밑 그림자·색상 이동 램프 12계열·소품 광원 통일·바닥 반복 깨기) · **③ 캐릭터**(위상 어긋내기·idle 호흡·눈 깜빡임·완료 팝·상태 틴트·걸음 스쿼시) · **④ 초대 7→3단계**(`/join/코드`·링크 복사·보직 자동 배정)


## 8/3 오전 — 미니게임 재설계 3차 + 실플레이 버그 수리 (전부 배포됨)

리서치 2건(리듬세상 Megamix / 원형별 유사 게임) 근거로 전면 재설계·긴장 패스:
- RHYTHM=콜앤리스폰스(판 분리 폐지) · DODGE=3레인 러너(3막·텔레그래프·아슬) · HOLD 틈 중앙(플래피 원형) · SORT ↑회전·Space하드드롭+고스트 · 긴장 패스 11종(SCRUB 딩·PLACE 콤보 회복·AUDIT 교환 함정·MASH 3구간·BALANCE 돌풍 예고·TRACE 물 차오름·TIMING 더블판정·TRACK 지뢰 콤보·SEARCH 비프·REACT 페이크·STILL 감시자) · 공통 "마지막 3초 심박"
실플레이 수리: 시간대 스킵 3종(이월 폐지·표 초기화·중복 토글) · 폰트 974자 재생성(글자 깨짐) · F10 토스트 미표시 · 토스트 위치(좌 패널 아래로) · 미니게임 중 이동 잠금 회귀 · 수첩 TAB 순환 · WorldLab F7 키 · 오늘의 기록에 일과 완료/놓침 수집 · 마감 큐 사용자 [확인] 전환+소대장 대사 확대 · 퇴소창이 판정을 덮던 순서(판정 확인 후 퇴소창, 26.4초 백스톱) · **B-1 비대칭 역할 강제 재수리**(watch 입력 원천 차단·SEQ 노출 후 사라짐·TRACE 진짜 정답 배치)
진행 중: HD-2D 목업 실험(별도 브랜치·프리뷰 배포 예정 — 본선 무영향, fps 실측용)

## 8/3 새벽 자동 파이프라인 — WORKORDER 실행 가능 항목 전부 소진

배치 5개(미니게임 2차 → B협동 → C·E → D·F·A-5 2차 → H·T)를 순차 배포했고 전부 해시 대조
완료. **남은 것은 전부 사람 몫이다**: PLAYTEST A/B(F9 전수·4인 협동)·번역 발주(H-3 본문)·
Sentry DSN 발급(env만 넣으면 켜짐)·데이터가 쌓여야 하는 밸런스 판단.
버림 확정: C-2·G단계·H-2(재론 안 함).

**마지막 배치(H·T) 검증 체크리스트:**
- [ ] 미니게임 전 원형이 키보드만으로 깨지는가(화살표 가상 커서/포커스 + Space) — SCRUB·AUDIT·TRACE·SEARCH·TRACK·SEQ 글자 변형
- [ ] 게임 화면 우상단 카메라 버튼 → PNG 다운로드(검은 화면이면 preserveDrawingBuffer 회귀 — 보고)
- [ ] F10 ScreenLab — 점호 7종·마감 큐·하달·종료 4종·토스트 17종·배너·게이지를 서버 없이 표시
- [ ] F11 WorldLab — 구역 텔레포트·날씨/주야 표시 오버라이드(배지)·VFX·캐릭터 뷰어·SFX 시청
- [ ] F9 확장 — [I]인터럽트·[P]연쇄판·[J]합동(가짜 인원)·[G]RaceBoard
- [ ] HUD 시간대 스킵 투표 버튼이 실제로 서버 voteSkip을 보내는가

## 다음 할 일 (다음 세션 — 순서대로)

**WORKORDER A~T의 코드 작업은 전부 완료·배포됐다** (C-2·G·H-2는 사용자 결정으로 버림).
남은 것은 ① HD-2D 목업 마무리 ② 사람 검증 ③ 후속 발주 후보다.

1. ~~HD-2D 목업~~ **완료·배포됨**: https://solider-a-day.vercel.app/hd2d/index.html
   (WASD 이동 · 토글 1=후처리 2=조명 3=내부해상도 · 좌상단 FPS/1% low 계측).
   **사용자 기기 실측 → 도입 여부 판단 대기.** 도입이면 전환 계획 발주, 미도입이면
   "느낌만" 연출 패스(비네트·틸트시프트 흉내를 본선 2D에) 후보.
   (원래 인수인계 절차 — 이미 완료됨:
   - 워크트리 `.claude/worktrees/agent-a954b4382907ebb06` (브랜치 worktree-agent-a954b4382907ebb06)
   - 세션 종료 시점에 그 워크트리에서 Unity 배치 빌드(`Hd2dMockBuildPlayer.Build` → unity/Build/hd2d)가
     돌고 있었다(첫 임포트라 느림). **빌드 완료 확인 → 산출물을 apps/web/public/hd2d/로 복사·커밋
     → 그 워크트리 루트에서 `npx vercel --yes`(프리뷰! --prod 금지) → URL/hd2d/index.html 전달.**
   - 구성: 실제 에셋 방 1개·원근 30~40°·포인트 라이트+블롭 그림자·내부해상도 0.66·틸트시프트 흉내·저강도 블룸.
     토글 1(후처리)/2(조명)/3(내부해상도) + FPS·프레임타임·1% low 계측기. 사용자가 기기에서 실측 후 도입 판단.)
2. **사람 검증(PLAYTEST)**: 아래 체크리스트. 특히 재설계 17종 F9 전수 + 2인 비대칭(SEQ·TRACE — 역할 강제
   재수리 후 미검증) + 마감 큐 [확인] 흐름 + 스킵.
3. **후속 발주 후보** (사람 검증 후 우선순위 결정):
   - 비대칭 판: 조작자 실수를 정답자 화면에 되먹임(서버 필드 `jointMistakes` 필요)
   - AUDIT 규칙 순차 공개(서버 데이터 필요)
   - 12종 대칭 합동에 역할 게이트·공동 부하 얹기(JOINT.md 잔여 — 사용자가 "각자 깨는 거냐" 물었음)
   - `chore-flag`(태극기) RHYTHM: bpm60·10초라 못 1개짜리 — quests.json bpm/workSeconds 조정
   - 소품 낱장 .meta 105장 WebGL 압축 오버라이드(아틀라스는 무관, H-1 발견)
   - Sentry DSN 발급(env만 넣으면 켜짐) · 번역 발주(사람 몫)
4. 워크트리 정리: `.claude/worktrees/`에 완료된 에이전트 워크트리 다수 — 병합 확인 후 `git worktree remove` 가능.

에이전트 운용 관례(사용자 지시): 오케스트레이터는 지휘만, 구현·검증은 Sonnet 병렬 워크트리,
파일 소유권을 발주에 명시해 충돌 0으로 병합, 빌드는 마지막에 한 번.


## 사람이 눈으로 확인할 것 (미검증 — 이 세션 마지막 배포분)

강력 새로고침(⌘⇧R) 후:

**D·F·A-5 2차 배치 (8/3 새벽 배포)**
- [ ] 소리 — 걸을 때 발소리 / 판 열림·닫힘음 / 위기 알람 / TIMING 접근음(가까울수록 높고 촘촘) / SEQ 단계별 계이름 / HOLD 통과 콤보 피치 상승
- [ ] 소품 — 같은 그림이던 소품들(서류함·약제함·잔반통 등 35종)이 코너 표식으로 구분되는가
- [ ] 신규 원형 — F9에서 STILL(부동자세: 참다가 "해산"에만 반응)·DODGE(3레인 회피)·RaceBoard([G]키, 고스트 진행 바) 동작하는가 / 실전에서 불시 점호=STILL·정신교육=STILL·보급 트럭=DODGE로 나오는가
- [ ] 온보딩 — 첫 1~2일 필수 일과가 기본 원형(문지르기·배치·대조 등)만 나오는가 / 이등병은 하달을 못 받고 사유가 뜨는가 / 취침 정산에 "내일 예고" 한 줄 / 청결 링에 판정 문턱선 / 1~2인 방은 기록에 안 남는가
- ⚠ 저장 v3 — 옛 저장 이어하기 불가(의도)

**C·E 배치 (8/3 새벽 배포)**
- [ ] 퇴소 화면 상단에 런 헤드라인("한파 속 물자 부족 D-5 완주" 형식)이 뜨는가 / 실패 시 결정타 수치("위생 18/20 · N일차부터")가 붙는가
- [ ] 점호 실패 시 조건별 소대장 대사가 뜨는가 / 후송 시 "남은 셋이 그 몫까지" 문장 / 돌발 시작에 상황 도입 문장 / 무전 끊김 문구가 통신병과 나머지에게 다른가
- [ ] 취침 정산의 군기 줄 아래 "합동무결 +12 · 선택미완 -8" 식 분해가 뜨는가
- [ ] 수첩(TAB)에 "오늘의 기록" 탭 — 그날 판정·후송·구제·보급이 시간순으로 쌓이는가
- [ ] (스키마만) 주간 변조 4종(한파·보급·검열·훈련)이 런마다 하나 배정 — 화면 노출은 아직 없음(후속)

**B단계 협동 (8/3 새벽 배포 — 4인/2인 실플레이 필요)**
- [ ] 합동판(SEQ·TRACE 계열)에서 한 명은 정답만·한 명은 조작만 보이는가 — 말 없이는 못 깨는가 (혼자면 기존 판 그대로여야 정상)
- [ ] 동료 스탯 붕괴 시 즉시 후송이 아니라 "쓰러졌다 — N초" 위기 토스트가 뜨고, 곁에서 E 홀드로 구조되는가 / 의무병이 더 빠른가 / 방치하면 기존 후송 그대로인가
- [ ] C키로 정형 문구 8종 휠이 열리고 숫자/클릭 발화 → 머리 위 말풍선이 뜨는가 / 연타 도배가 안 되는가(2초 쿨다운)
- [ ] 분대장 수첩에서 필수 퀘스트 옆 "구제 발동" 버튼 → 필수→선택 강등되는가 / 자격 없는 사람은 사유 토스트가 뜨는가
- [ ] 판정 화면 구제 문구가 "발동" 어조로 바뀌었는가 (자동 상쇄 문구가 아니라)
- ⚠ 저장 포맷 v3 — 진행 중이던 저장 런 이어하기 불가(의도된 무효화)

**미니게임 전면 개편 2차 (8/3 새벽 배포 — F9에서 판별 가능)**
- [ ] PLACE 정돈·배치 — 타일이 좌우로 미끄러지다 클릭/Space에 떨어져 쌓이는가 / 어긋난 만큼 잘려 다음 타일이 좁아지는가 / 배터리 극성(무전기)은 완전 빗나감에 즉시 실패인가
- [ ] HOLD 계기 유지 — 눌러서 붕 뜨고 위/아래 장애물을 피하는가 / 충돌 시 0.8초 무적 깜빡임 후 재개인가
- [ ] SORT 분류 — 테트리스인가(←→ 이동, 클릭/Space 회전, 줄 지우기 목표) / 천장 닿으면 실수 1 + 판 비움인가
- [ ] RHYTHM 박자 — 망치 시연을 보고 같은 리듬으로 따라 박는가 / 엇박이면 못이 삐뚤게 박히는가
- [ ] TRACK — 풍선이 한 번에 하나씩 랜덤 위치에 뜨고 클릭으로 터트리는가 / 수명 끝 0.4초 깜빡임·수축
- [ ] REACT 즉시 반응 — 쉘이 초록("대기")→빨강("지금!")으로 바뀔 때 누르는 방식인가
- [ ] 실험대에서 판 시작 클릭이 판의 첫 입력으로 새지 않는가(SEARCH 헛짚기·AUDIT 오답으로 안 잡히는가)
- [ ] 미니게임 목록의 SEARCH 이름이 "수색"으로 보이는가

**A-5 1차 미니게임 (앞 배포 — F9 실험대에서 판별 가능)**
- [ ] SCRUB — 얼룩이 문지를수록 옅어지다 벗겨지는가(한 번 스치면 끝이 아니라) / 덩어리 하나를 다 닦으면 테두리 번쩍+탭 소리 / 남은 5%에서 은은한 펄스 / 세면장(mirror)에서 80% 넘게 닦으면 파란 예고 깜빡임 0.5초 후 한 덩어리에 김이 다시 서리는가
- [ ] SCRUB 순수 청소(바닥·거울 등)에서 깨끗한 곳을 헛문질러도 실수가 안 쌓이는가 (모래·방역은 약제 낭비 시 기존대로 실수)
- [ ] SCRUB·SEARCH 시간초과 시 "시간 종료 — 한 만큼 인정"+등급(B/C)으로 통과하는가 (절반 미만이면 "절반은 채워야 인정된다" 재시도)
- [ ] AUDIT — 하단 "이상 없음 보고" 버튼이 모든 판에 있는가 / 여러 판 돌리면 가끔 어긋남이 진짜 없는 판이 나오는가 / "적발 n/m"의 총수(m)가 화면에서 사라졌는가
- [ ] SEARCH — 못 찾고 끝나면 1.4초간 놓친 자리가 빨갛게 깜빡이는가 / 찾는 순간 "관물대 밑 편지" 같은 카드가 뜨는가
- [ ] TRACE — 관이 딸깍 소리와 함께 0.1초 돌아가는가(즉시 스냅이 아니라)
- [ ] 연속 A를 2번 이상 받으면 판 머리에 "연속 A ×n — 시간 −n%" 배지가 뜨고 실제로 빡빡해지는가 / A 아닌 등급이 나오면 풀리는가
- [ ] 하루 일과표에서 같은 원형(문지르기 등)이 연달아 붙어 나오는 일이 줄었는가

**아트·캐릭터 (이번 배포의 큰 변화)**
- [ ] 화면 첫인상 — 캐릭터·소품 발밑에 그림자가 생겨 바닥에 붙어 보이는가
- [ ] 복도·연병장 바닥 — 균열·얼룩으로 반복이 깨졌는가 / 색이 이상하게 튀는 곳은 없는가(램프 교체 회귀)
- [ ] 분대원 4인의 idle 호흡이 서로 다른 박자인가 (같으면 실패)
- [ ] 눈 깜빡임(정면 2~5초 간격) / 일과 완료 순간 몸이 1.15배 튀는가 / 동상=푸른 틴트·탈진=탁한 틴트
- [ ] 걸을 때 발 디딤에 맞춰 몸이 눌리고 먼지가 퍽 튀는가 / 픽셀 지글거림이 거슬리면 보고(스케일→오프셋 대안 준비됨)

**게임이 말한다**
- [ ] 하달하면 받는 사람 머리 위 말풍선("또 나야?" 등) / 하달 거부 시 사유가 뜨는가
- [ ] 구제권 1장 이하일 때 일과표·수첩에 경고가 상시 떠 있는가
- [ ] 퇴소 화면에 실패 조건 수치 + 소대장 대사가 뜨는가
- [ ] 설정에서 UI 배율 125/150%가 실제로 게임 화면에 먹는가 / 150%에서 겹치는 화면 있으면 보고
- [ ] 미니맵 분대원 마커가 보직별로 모양이 다른가 (원·사각·십자·삼각)

**초대**
- [ ] 대기실 "초대 링크 복사" → 새 브라우저에서 링크 열면 이름만 넣고 입장되는가 (클립보드는 HTTPS 필요 — 실도메인에서)
- [ ] 재방문 시 이름이 자동으로 채워지는가

**앞서 배포됐지만 아직 미확인**
- [ ] 하달 창에서 전원 확정하면 즉시 닫히고 시간이 재개되는가 / 정지 중 "하달 대기 (N초)" 표시
- [ ] 시계가 이유 없이 멈추면 → 배너("재접속 중 n/4차")가 뜨는지. **배너 없이 멈추면 새 버그, 보고**
- [ ] F9 실험대 — 판 고르면 "클릭하면 시작" 대기 → 첫 입력부터 시간. 분류 "4번" 통 사라짐, 계기 유지 첫 클릭부터
- [ ] 점호 판정 6.4초 → 승급 → 취침 순서로 흐르는가 / 구제권 사용 시 주황 문구

**아는 한계 (버그 아님)**
- 그림자는 `_box()` 계열 소품에만 — 나무·깃대·모래주머니 등 14종 커스텀 소품은 아직 없음
- 텔레포트 없이 훈련장까지 걸어가야 함 (WorldLab은 T단계)
- csharpgen `reliefsUsed` 골든 테스트 1건, sim `quests.ts` CareTemplate 타입 1건 — 세션 전부터 빨간 기존 이슈


## 알려진 문제 (이월)

- 소품 낱장 .meta 105장의 WebGL 오버라이드가 Compressed로 박혀 있음 — Sprite2DImport가 Default만 강제. 아틀라스는 비압축 명시라 무관하나 낱장 잔존분 확인 필요 (H-1 발견)
- ~~InterruptBoard TimedOut 관통~~ **해소됨**(A-5 2차)
- **InterruptBoard가 TimedOut/GradesOnTimeout을 관통 안 함** (구 기록 — 위 참조) — 래퍼가 내부 판의 통과·실패를
  자기 Clear/Fail로 다시 선언하는 구조. 지금은 REACT 인터럽트가 BALANCE·TRACE(둘 다 하드
  판정)에만 걸려 증상 없음. A-5 2차에서 과정형에 인터럽트를 얹기 전에 래퍼에 위임을 넣을 것
- **저장 포맷 v2** — 이번 배포로 진행 중이던 저장 런은 이어하기 불가(의도된 무효화)
- 공통 일과가 오전 칸에만 뜨는 것(`pickPhase` 인덱스 0 고정) — 의도인지 실수인지 미확인
- `optionalMissed` 페널티 건수 비례 아님 — WORKORDER 침묵감사 #12
- CORS `*` — 도메인 확정 후 좁히기
- 워크트리 잔재: `.claude/worktrees/`에 세션 한도로 죽은 랩 에이전트 3개의 미완 워크트리가 있을 수 있음 — T단계 시작 때 참고하거나 지울 것


## 명령 (변한 것 없음 — 요지만)

```bash
npm run -w services/gameserver dev     # :8080
npm run dev -w apps/web                # :3000
python3 tools/sprites/generate.py      # 아트 재생성 (2회 실행 비트 동일해야 정상)
# 씬: BaseScene.CreateScene / 빌드: BuildPlayer.Web (성공 신호는 "Exiting batchmode successfully")
node tools/webgl-sync.mjs              # 빌드 후 필수 — 빼먹으면 화면이 옛것
npx tsx tools/csharpgen/src/index.ts   # 프로토콜 고쳤으면 필수
npx vercel --prod --yes                # 웹 배포 — push가 Render 서버 배포 트리거
# 배포 후: 로컬/원격 web.loader.js sha256 대조 (관례)
```

## 참고 문서

| | |
|---|---|
| `docs/WORKORDER.md` | **발주서 원본** — 다음 할 일의 상세는 전부 여기 |
| `docs/BENCHMARK.md` | 리서치 16건 종합 — 왜 이 순서인지의 근거 |
| `docs/JOINT.md` · `docs/game_spec.md` · `docs/QUESTS.md` | 합동 설계 · 미니게임 원형 · 일과(생성물) |
| `SAD-GDD-002_2D기획서.html` | 기획서 — 구제권·점호 6.4초·사운드 §16 등 코드와 대조할 원본 |
